# MD Block 版本记录

本文件记录 MD Block 每个正式版本的功能变化、兼容性、验证结果和发布状态，按新版本在前排列。

## 维护约定

- 每次正式发布后新增一个版本条目，并同步 `package.json` 中的版本号。
- 必须记录发布日期、发布状态、新增功能、问题修复、数据兼容和已知限制。
- 涉及 Widget 数据结构时，必须保留既有 `useSyncedState` 键，并记录迁移方案。
- 发布前从项目目录执行 `npm run typecheck` 和 `npm run build`，记录验证结果。
- 不修改 `manifest.json` 中既有 Widget ID，避免旧实例失去升级和数据迁移能力。

---

## V0.2.3

- 日期：2026-09-09
- 发布状态：待发布
- 类型：编辑窗口与中文排版优化

### 编辑窗口

- 默认打开尺寸调整为宽 480 px、高 640 px，减少对 Figma 画布的遮挡。
- 目录默认折叠，仍可通过顶部目录按钮手动展开；窗口缩小到 560 px 以下时继续自动折叠。

### Markdown 渲染

- 修复空白有序列表项、无序列表项或引用内容把 `<br />` 显示为正文的问题。
- `<br />`、仅有标题标记等视觉空白内容统一渲染为空白占位，不显示 Markdown 兼容语法。
- 修复有序列表中的单个 Figma 节点链接被压缩后逐字纵向换行的问题，链接文字现在填满列表剩余宽度。
- 文档标题与 Markdown 各级标题统一使用 700 字重，增强标题层级识别。

### 中文字体

- 编辑弹窗使用系统中文字体栈：macOS 优先 PingFang SC，Windows 优先 Microsoft YaHei，并提供 Noto Sans SC 与系统字体回退。
- Figma Widget 画布受 API 限制不能按操作系统选择本机字体，统一使用受支持的 Google Font `Noto Sans SC`，保证跨平台中文显示一致。
- 代码块继续使用等宽字体 `Roboto Mono`。

### 数据兼容

- 不新增或修改同步状态字段，继续使用 `schemaVersion = 3`。
- 保留 `title`、`markdown`、`assets` 和 `figmaFileKey`，旧版 Block 数据无需迁移。

### 验证结果

- `npm run typecheck`：通过。
- `npm run build`：通过。
- 空白有序列表、无序列表和引用内容解析回归：通过。
- 单节点链接列表布局与标题字重：通过类型检查和构建，待 Figma 本地运行验证。
- 默认窗口尺寸、目录折叠与画布字体：待 Figma 本地运行验证。

---

## V0.2.2

- 发布日期：2026-09-07
- 发布状态：已发布
- 类型：画布节点链接修复

### Figma 画板与图层链接

- 新插入的画板链接使用 Figma 规范的完整节点地址，并对文件名括号进行严格 URL 编码，避免 Markdown 截断和 Not Found。
- 画布 Widget 兼容解析旧版带括号文件名的链接，以及 V0.2.1 测试期间生成的不含文件名路径段的简化链接。
- Figma 节点链接不再依赖 `Span href` 打开浏览器，而是通过 Widget 点击事件直接切换页面、选中节点并缩放定位。
- 普通网页链接仍保留标准超链接行为；正文、标题、列表、引用和表格内的 Figma 节点链接均支持画布内定位。
- 节点链接点击会抑制 Block 根节点的编辑器点击事件，避免先打开编辑弹窗再定位画板。

### 数据兼容

- 不新增或修改同步状态字段，继续使用 `schemaVersion = 3`。
- 保留 `title`、`markdown`、`assets` 和 `figmaFileKey`，已有 Block 内容和文件级链接配置不变。
- 新旧格式的现有节点链接在渲染时自动兼容，无需删除后重新插入。

### 验证结果

- `npm run typecheck`：通过。
- `npm run build`：通过。
- 新旧节点链接解析：通过自动化回归检查。
- 画布内跨页面定位且不打开编辑弹窗：用户本地验证通过。

### 已知限制

- 目标画板或图层被删除后无法继续定位，点击时会显示错误提示。
- 使用者仍需拥有目标 Figma 文件及节点的访问权限。

---

## V0.2.1

- 发布日期：2026-09-07
- 发布状态：已发布
- 类型：公开版文件链接获取与复用

### 公开版文件链接方案

- 公开版不再依赖仅私有资源可用的 `figma.fileKey`，首次使用时通过粘贴当前 Figma 文件链接获取文件 Key。
- 支持个人草稿及团队文件的 `design`、`file`、`proto` 和 `board` 链接格式，不依赖组织内部 API。
- 独立绑定入口从主工具栏移除；右键“插入画板”按钮可打开“绑定 Figma 链接”。
- 未绑定时点击“插入画板”会自动打开绑定弹窗，绑定成功后继续完成本次插入。
- Widget 复制或派生到另一个 Figma 文件后，可重新绑定新文件链接，避免继续生成原文件地址。

### Figma 文件链接复用

- 画板链接由“每个 MD Block 单独绑定”调整为“每个 Figma 文件绑定一次”。
- 同一个人草稿或团队文件内的所有 MD Block 自动复用文件链接配置。
- 文件级配置保存在 Figma `DocumentNode` 的插件数据中，由相同 Widget ID 私有读取，不产生网络请求。
- 同一文件的协作者可复用已绑定配置，无需按账号重复粘贴。
- 不采用账号级或团队级全局绑定，避免多个 Figma 文件共用错误的文件 Key。

### 旧版兼容与数据迁移

- 保留 `title`、`markdown`、`assets` 和 `figmaFileKey` 等既有同步状态键。
- 数据结构版本升级到 `schemaVersion = 3`，兼容早期以 `content` 保存 Markdown 的实例。
- 新版实例打开时扫描当前页面内相同 Widget ID 的旧实例，保留未知字段并通过 `setWidgetSyncedState` 更新界面。
- V0.2.0 的逐 Widget 文件 Key 可自动迁移为当前文件的共享配置。
- 当同一页面旧实例包含多个冲突的文件 Key 时不自动选择，要求用户重新绑定以防止误链。

### 发布配置

- 移除 `enablePrivatePluginApi`，支持公开发布。
- `documentAccess` 使用 `dynamic-page`；Widget 不访问外部网络，`allowedDomains` 为 `none`。
- 保持 Widget ID `1671425799803301634` 不变，保障旧实例获得新版界面并保留数据。

### 验证结果

- `npm run typecheck`：通过。
- `npm run build`：通过。
- 个人草稿文件链接绑定、画板插入及旧实例数据保留：用户本地验证通过。

---

## V0.2.0

- 发布日期：2026-09-02
- 发布状态：已发布
- Widget 名称：MD Block
- Widget ID：`1671425799803301634`
- 需求文档：[Figma-MD V0.2.0 功能需求](docs/需求/Figma-MD%20V0.2.0%20功能需求.md)

### 编辑器与界面

- 编辑窗口默认根据 Figma 文件权限进入编辑或查看状态。
- 移除独立的预览、编辑切换按钮。
- 标题、保存状态、导入、插入画板、复制和下载操作统一到顶部工具栏。
- 目录支持完全折叠，折叠后不再保留占位竖栏。
- 正文左右边距保持一致，取消页面底部无效横向滚动条。
- 行移动手柄靠近正文；点击打开紧凑的区块插入菜单，按住可拖动当前行。
- 图标统一视觉风格，并在悬停 1 秒后显示操作名称。
- 错误提示增加关闭按钮。
- 修复长内容编辑后关闭窗口，再次点击 Widget 无响应的问题。

### Markdown 能力

- 支持标题、正文、引用、列表、任务列表、代码块、分隔线、表格、链接和图片等常用区块。
- 当前标题行显示 Markdown 标记，离开后恢复渲染效果。
- 空白行保持空白，不再显示 `##` 或 `<br />` 等语法字符。
- 链接以蓝色下划线样式展示。
- 链接内部或紧邻链接输入普通文字时继续扩展链接标题；在链接边界输入空格后退出链接样式。
- 修复 GFM 表格导出后无法被标准 Markdown 编辑器识别的问题。
- 新增 Markdown 文件导入，导入内容追加到当前文档末尾。
- 下载时支持将 Markdown 与图片附件打包导出。

### 表格

- 修复画布 Widget 中表格无法正确渲染的问题。
- 表格根据画布宽度自动分配列宽，所有单元格填满当前行高度。
- 恢复“在下方添加行”和“在右侧添加列”操作。
- 暂停列宽拖拽，优先保证表格输入稳定。
- 使用原生 ProseMirror 表格输入，修复中文输入法将拼音原文与汉字同时保留的问题。

### 图片

- 新插入图片默认铺满正文宽度，并随编辑区域宽度自适应。
- 支持拖动图片右下角手柄调整大小。
- 手动调整后的图片保存固定像素宽度，不再跟随页面宽度缩放。

### Figma 画板与图层链接

- 支持将当前选中的画板、组或图层以 Markdown 链接插入。
- 链接标题使用所选节点名称，点击后可定位到对应节点。
- 链接插入到当前编辑光标位置；选中文字时将选中文字转换为链接。

### 发布配置

- `documentAccess` 使用 `dynamic-page`。
- Widget 不访问外部网络，`allowedDomains` 为 `none`。
- 发布入口文件为 `dist/widget.js` 和 `dist/index.html`，无需上传 ZIP。

### 验证结果

- `npm run typecheck`：通过。
- `npm run build`：通过。
- UI 单文件构建：通过。
- Widget JavaScript 构建：通过。

### 本地构建命令

```bash
cd "/Users/zhengzhi/Library/Mobile Documents/com~apple~CloudDocs/ZhengZhi/Code/Product/Figma-MD"

npm run typecheck

npm run build
```
